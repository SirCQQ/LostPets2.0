function showMap(){
    let card= document.getElementsByClassName('pet-img')
    console.log(card);
    console.log(card.length)
    for(let i=0;i<card.length;i++)
        {
            console.log(card[i])
        }
}